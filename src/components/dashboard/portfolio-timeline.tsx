"use client";

import type { Id } from "@convex/_generated/dataModel";

// Phase F stub — workspace portfolio timeline: one row per project (list)
// on a shared date axis, with progress fill and health color. Implemented
// by the portfolio workstream.

export function PortfolioTimeline(props: {
  workspaceId: Id<"workspaces">;
}) {
  void props;
  return null;
}
