# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts.bak

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Note: convex/_generated/ is committed. The CLI (`convex dev` / `convex deploy`)
# regenerates these files in place; they're tracked so a fresh checkout
# typechecks without needing the Convex CLI to be configured first.

# Service worker is generated by @serwist/next at build time.
public/sw.js
public/sw.js.map
public/swe-worker-*.js

# Capacitor native projects (generated by `npx cap add ios|android`).
# Commit these once you've configured them for your team.
/ios
/android

# IDE
.idea/
.vscode/

# One-off verification scratch: a throwaway harness an agent builds to
# answer a single question ("does the Chart chapter offer shapes for a
# built-in?"). Useful while it runs, meaningless the moment it has answered,
# and committing it would leave a second half-built gallery beside the real
# one for somebody to mistake for the harness.
.verify/
verify-shape.tmp.mjs
