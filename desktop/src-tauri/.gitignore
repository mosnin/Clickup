# Build output. Large, machine-specific, regenerated by `cargo tauri build`.
/target/

# Generated by `cargo tauri icon` from a source image not yet in the repo.
# Tracked once the icon exists — see README.
/icons/
