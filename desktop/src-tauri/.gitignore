# Build output. Large, machine-specific, regenerated by `cargo tauri build`.
/target/

# Keep the canonical app icon; ignore any additional platform variants until
# they are intentionally selected and reviewed.
/icons/*
!/icons/icon.png
!/icons/icon.icns
